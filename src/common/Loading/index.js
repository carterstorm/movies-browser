import {
    LoadingContainer,
    LoadingSpinner
} from "./styled";

export const Loading = () => {
    return (
        <LoadingContainer>
            <LoadingSpinner />
        </LoadingContainer>
    );
};